name: shared_preferences_riverpod
description: help to use shared_preferences with riverpod.
version: 0.1.1
homepage: https://github.com/gamako/shared_preferences_riverpod

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.0.8
  riverpod: ^1.0.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  flutter_riverpod: ^1.0.0
  shared_preferences_platform_interface: ^2.0.0

flutter:

